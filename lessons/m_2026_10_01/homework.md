# Домашнее задание — 01.10.2026

**Тема:** типы данных в Python.

## Что нужно сделать — коротко

1. Прочитай теорию ниже (часть 1). Примеры можно копировать в `test.py` и запускать.
2. Реши задачи (часть 2). Они лежат **в этой же папке** в файлах `task_....py`.
   Решаешь **прямо в этих файлах**, новые файлы создавать не нужно.
3. Сделай коммит и отправь на GitHub — как в `readme.md`.

Порядок файлов с задачами — такой же, как порядок тем в теории:

| № | Файл | Тема |
|---|---|---|
| 1 | `task_str.py` | строки |
| 2 | `task_int.py` | целые числа |
| 3 | `task_float.py` | дробные числа |
| 4 | `task_bool.py` | `True` / `False` |
| 5 | `task_none.py` | `None` |
| 6 | `task_list.py` | списки |
| 7 | `task_convert.py` | превращение типов |

---

# Часть 1. Теория

Тип данных — это **что за значение** лежит в переменной и **что с ним можно делать**.
С числами можно считать, со строками — склеивать. Если перепутать типы, Python выдаст ошибку.

Узнать тип можно всегда:

```python
print(type(5))        # <class 'int'>
print(type("привет")) # <class 'str'>
```

---

## 1. `str` — строка (текст)

Текст в кавычках. Кавычки одинарные и двойные равноправны.

```python
name = "Себастьян"
print(len(name))              # 9 — длина строки
print(name + "!")             # Себастьян! — склейка
print(f"Привет, {name}!")     # Привет, Себастьян! — f-строка
print(name.upper())           # СЕБАСТЬЯН
print(name[0])                # С — нумерация символов с нуля
print(name[-1])               # н — последний символ
```

**f-строка** — главный способ собрать текст: ставишь `f` перед кавычками,
а переменные пишешь в фигурных скобках `{}`.

**Важно:**
- Всё, что вводит пользователь через `input()`, — строка, даже если он ввёл число.
- Номер телефона и почтовый индекс хранят строкой: там бывают `+` и ведущие нули.
  Правило: **если с данными не нужна математика — это строка.**
- Строку нельзя изменить по частям. `.upper()` не меняет старую строку,
  а возвращает **новую**.

---

## 2. `int` — целое число

Числа без дробной части: `0`, `7`, `-15`.

```python
a = 7
b = 2
print(a + b)   # 9
print(a - b)   # 5
print(a * b)   # 14
print(a / b)   # 3.5  — обычное деление, всегда даёт float
print(a // b)  # 3    — целая часть от деления
print(a % b)   # 1    — остаток от деления
print(a ** b)  # 49   — возведение в степень
```

`//` и `%` нужны чаще, чем кажется. Перевести 130 секунд в «2 мин 10 сек»:

```python
seconds = 130
print(seconds // 60)   # 2  — сколько целых минут
print(seconds % 60)    # 10 — сколько секунд осталось
```

Проверить, чётное ли число: `число % 2` — если остаток `0`, число чётное.

---

## 3. `float` — дробное число

Числа с точкой: `3.14`, `0.5`, `-2.0`. Разделитель — **точка**, не запятая.

```python
price = 99.9
print(price * 2)           # 199.8
print(round(3.14159, 2))   # 3.14 — округление до 2 знаков после точки
```

**Главная странность, которую надо просто знать:**

```python
print(0.1 + 0.2)          # 0.30000000000000004, а не 0.3
print(0.1 + 0.2 == 0.3)   # False !
```

Компьютер хранит дроби приблизительно. Поэтому:
- для вывода дробные числа **округляют**: `round(0.1 + 0.2, 2)` → `0.3`;
- дробные числа **не сравнивают через `==`** напрямую — сначала округляют.

---

## 4. `bool` — логический тип

Всего два значения: `True` (да) и `False` (нет). Появляются сами при сравнениях.

```python
age = 15
print(age > 18)        # False
print(age >= 15)       # True  — «больше или равно»
print(age == 15)       # True
print(age != 15)       # False — «не равно»

print(True and False)  # False — «и»: нужно, чтобы оба были True
print(True or False)   # True  — «или»: достаточно одного
print(not True)        # False — «не»: переворачивает
```

Результат сравнения можно сохранить в переменную:

```python
is_adult = age >= 18
print(is_adult)        # False
```

Не путай: `=` — присвоить значение, `==` — сравнить.

---

## 5. `None` — «значения нет»

Это не ноль и не пустая строка, а специальная пометка «ничего не известно / не задано».

```python
middle_name = None
print(middle_name is None)       # True
print(not (middle_name is None)) # False — «значение есть?»
```

Проверяют его через `is None`, а не через `==`.

Разница важна: если в поле «сколько денег» лежит `0` — денег нет.
Если лежит `None` — мы просто **не знаем**, сколько там.

---

## 6. `list` — список

Несколько значений в одной переменной, по порядку, в квадратных скобках.

```python
marks = [5, 4, 5, 3]

print(marks[0])       # 5  — первый элемент, нумерация с нуля
print(marks[-1])      # 3  — последний
print(len(marks))     # 4  — сколько элементов

marks.append(4)       # добавить в конец
marks[1] = 5          # заменить элемент — список изменяемый!
print(marks)          # [5, 5, 5, 3, 4]

print(sum(marks))     # 22 — сумма
print(max(marks))     # 5  — максимум
print(min(marks))     # 3  — минимум
print(sorted(marks))  # [3, 4, 5, 5, 5] — отсортированная копия
```

В списке могут лежать любые типы, в том числе разные сразу: `["Себастьян", 15, True]`.

---

## 7. Превращение одного типа в другой

Это самое частое место ошибок у новичков.

```python
age_text = "15"           # это СТРОКА (так приходит из input())
age = int(age_text)       # теперь число, можно считать
print(age + 1)            # 16

print(str(15) + " лет")   # 15 лет — число -> строка, чтобы склеить
print(float("3.5"))       # 3.5 — строка -> дробное число
print(int(3.9))           # 3 — дробная часть отбрасывается, без округления!
```

Что будет, если типы перепутать:

```python
print("15" + 1)   # TypeError — строку и число сложить нельзя
```

А если сложить две строки, они просто склеятся: `"15" + "1"` → `"151"`.

---

## 8. Что ещё бывает (просто знать, разберём позже)

- **`Decimal`** — «честные» дроби для денег. Там, где `float` даёт `0.30000000000000004`,
  `Decimal` считает копейки точно.
- **`datetime`** — дата и время как отдельный тип. Например, можно узнать,
  сколько дней осталось до Нового года.

---

## Шпаргалка

| Тип | Что хранит | Пример | Особенность |
|---|---|---|---|
| `str` | текст | `"Привет"` | нельзя изменить по частям |
| `int` | целое число | `42` | `//` и `%` для деления нацело |
| `float` | дробное число | `3.14` | считает неточно, округляй через `round` |
| `bool` | да / нет | `True` | результат сравнений |
| `None` | «значения нет» | `None` | проверять через `is None` |
| `list` | список значений | `[1, 2, 3]` | изменяемый, индексы с нуля |

**Три правила:**
1. `input()` всегда возвращает строку — для математики оборачивай в `int()` или `float()`.
2. `float` не сравнивают через `==` напрямую.
3. Не знаешь, что в переменной, — напечатай `type(переменная)`.

---

# Часть 2. Задачи

В каждом файле `task_....py` — **2 задачи**. Каждая задача устроена одинаково:

```python
# ---------- Задача 1 ----------
# Условие: что нужно сделать.
#
# ОЖИДАЕМЫЙ ВЫВОД:
# то, что должно напечататься
print("--- Задача 1 ---")
apples = 17          # <- готовые данные, их НЕ меняй

# ТВОЙ КОД ЗДЕСЬ
```

## Как решать — по шагам

1. Открой файл, например `task_str.py`.
2. Прочитай **Условие** задачи.
3. Напиши свой код **под строкой `# ТВОЙ КОД ЗДЕСЬ`**. Больше ничего в файле не трогай.
4. Запусти файл. В VS Code — кнопка ▶ в правом верхнем углу. Или в терминале,
   находясь в папке репозитория:

   ```bash
   python lessons/m_2026_10_01/task_str.py
   ```

   (если команда `python` не найдена — пиши `python3`)
5. Сравни то, что напечаталось **под строкой `--- Задача 1 ---`**, с блоком
   **ОЖИДАЕМЫЙ ВЫВОД**. Должно совпасть до символа.
6. Совпало — переходи к следующей задаче. Не совпало — ищи ошибку.

## Если застрял

- Перечитай нужный раздел теории выше — всё, что нужно для задач, там есть.
- Появилась ошибка (красный текст) — прочитай **последнюю строку**, там написано, что не так.
- Не получилось совсем — оставь свою попытку в файле, разберём на уроке.
  Пустая задача хуже, чем задача с ошибкой.

## Как сдать

Когда все 7 файлов решены — сделай коммит (например, `Решить задачи на типы данных`)
и отправь на GitHub, как написано в `readme.md`.
